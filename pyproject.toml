[project]
name = "recall"
version = "0.1.0"
description = "Local full-text search over your bookmarks, snippets and notes"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
recall = "recall.cli:main"
rc = "recall.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"